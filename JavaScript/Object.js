//Objetos en Java Script

// Un objeto es una agrupacion de Datos

const miPrimerObjeto = {
    //Asi se define un objeto
    // Objeto vacio, no contiene ningun dato
}

// Objeto 2
// Objeto con datos, los objetos contienen propiedades
const miObjeto = {
    // 1 Propiedad = variable dentro de un objeto
    unNumero: 12,
    unString : "Cadena de Caracteres, de un Objeto",
    unaCondicion : true
    // Para definir las variables dentro del objeto no se utiliza let

}

console.log(miObjeto) // Imprimiendo el objeto completo
console.log(miObjeto.unString) // Imprimiendo el objeto y su propiedad
